<h1>id: <% @massage.id %> のメッセ委ℤ－編集ページ</h1>

<%= form_with(model: @massage,local: true)do |f| %>
    <%= f.label :content,"メッセージ" %>
    <%= f.text_field :content %>
    
    
    <%= f.submit "投稿" %>
<% end %>


<%= link_to "一覧に戻る", massage_path %>
