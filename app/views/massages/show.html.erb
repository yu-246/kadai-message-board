<h1>id: <%= @massage.id %> のメッセージ詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @massage.id %></td>
  </tr>
  <tr>
    <th>タイトル</th>
    <td><%= @massage.title %></td>
  </tr>
  <tr>
    <th>メッセージ</th>
    <td><%= @massage.content %></td>
  </tr>
</table>


<%= link_to 'このメッセージを編集する', edit_massage_path(@massage), class: 'btn btn-light' %>
<%= link_to "このメッセージを削除する", @massage, method: :delete, data: { confirm: "本当に削除してよろしいでしょうか？"}, class: "btn btn-danger"  %> 